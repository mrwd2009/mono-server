import { TextAlign, TextVerticalAlign } from "../core/types";

export interface TextPositionCalculationResult {
  x: number;
  y: number;
  align: TextAlign;
  verticalAlign: TextVerticalAlign;
}

export function parsePercent(value: any, maxValue: any): number {
  return 1;
}

export function calculateTextPosition(out: any, pots: any, rect: any) {

}